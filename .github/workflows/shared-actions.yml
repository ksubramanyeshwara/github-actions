name: Shared Actions using GitHub Marketplace

on:
  workflow_dispatch:

jobs:
  job-checkout-repo:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3
      - run: echo "Repository checked out successfully!"
      - run: ls -la
